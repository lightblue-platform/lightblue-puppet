{
  "datasource" => {
    rdbms => { 
      "jndi-name => "<%= jndi_name %>",
      "connection-url" => "<%= connection_url %>",
      "driver-name" => "<%= driver_name %>",
      "user-name" => "<%= user_name %>",
      "password" => "<%= password %>",
    }
  }
}